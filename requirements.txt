# Core RAG dependencies
chromadb>=0.5.0
sentence-transformers>=3.0.0
streamlit>=1.35.0
python-dotenv>=1.0.0

# Document parsing
pymupdf>=1.24.0          # PDF support (import as 'fitz' or 'pymupdf')
python-docx>=1.1.0       # DOCX support

# HTTP client (for Ollama & LM Studio)
requests>=2.31.0

# AI provider SDKs (install only the ones you need)
openai>=1.30.0            # OpenAI
anthropic>=0.28.0         # Claude (Anthropic)
mistralai>=1.0.0          # Mistral AI

# Utilities
numpy>=1.26.0
